import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Loader } from '../ui';

<Meta title="Loader" component={Loader} />

# Loader

<Canvas>
<div style={{width: 200, height: 200, position: 'relative'}}><Loader /></div>
<div style={{width: 200, height: 200, position: 'relative'}}><Loader size={100} /></div>
</Canvas>