import { Banner, Button } from '../../src/ui';
import { RefreshingIcon } from '../../src/Icons/jsx';

# Banner

## Needs Attention

<Banner action={<Button scale="small" color="secondary">Update Billing Method</Button>}>
    <b>Payment failed.</b> Please update your billing method.
</Banner>

```
<Banner action={<Button scale="small" color="secondary">Update Billing Method</Button>}>
    <b>Payment failed.</b> Please update your billing method.
</Banner>
```

## Secondary

<Banner type="secondary" action={<Button scale="small" color="primary" outline>Resend Email</Button>} icon={<RefreshingIcon />}>
    Please confirm your email by clicking the link we sent to <b>email</b>
</Banner>

```
<Banner type="secondary" action={<Button scale="small" color="primary" outline>Resend Email</Button>} icon={<RefreshingIcon />}>
    Please confirm your email by clicking the link we sent to <b>email</b>
</Banner>
```

## Readonly

<Banner type="readonly">
    <b>Payment failed.</b> Please update your billing method.
</Banner>

```
<Banner type="readonly">
    <b>Payment failed.</b> Please update your billing method.
</Banner>
```
