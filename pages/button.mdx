import Button from '../src/Button/Button';
import Icon from '../internal/Icons/Play';

# Buttons & CTA

<br /><br />

## Sizes

<Button color="primary" style={{width: 200, margin: 8}}>Default</Button>
<Button color="primary" style={{width: 200, margin: 8}} size="medium">Medium</Button>
<Button color="primary" style={{width: 200, margin: 8}} size="small">Small</Button>

```
<Button color="primary">Default</Button>
<Button color="primary" size="medium">Medium</Button>
<Button color="primary" size="small">Small</Button>
```

<br /><br />

## With Icon

<Button color="primary" style={{margin: 8}} iconRight={<Icon />}>
    Default
</Button>

<Button color="primary" style={{margin: 8}} iconRight={<Icon />}>
    Default
</Button>

```
<Button color="primary" style={{margin: 8}} iconRight={<Icon />}>
    Default
</Button>

<Button color="primary" style={{margin: 8}} iconLeft={<Icon />}>
    Default
</Button>
```

<br /><br />

## Primary action buttons

Used for main action on current page, usually it’s one of two, either Free Download or Buy Now

<br />

### Primary - High priority

<Button color="primary" style={{width: 200, margin: 8}}>Primary</Button>
<Button color="primary" style={{width: 200, margin: 8}} loading>Loading</Button>
<Button color="primary" style={{width: 200, margin: 8}} disabled>Disabled</Button>

```
<Button color="primary">Primary</Button>
<Button color="primary" loading>Loading</Button>
<Button color="primary" disabled>Disabled</Button>
```

<br /><br />

### Primary - Low priority

<Button color="primary" outline style={{width: 200, margin: 8}}>Primary</Button>
<Button color="primary" outline style={{width: 200, margin: 8}} loading>Loading</Button>
<Button color="primary" outline style={{width: 200, margin: 8}} disabled>Disabled</Button>

```
<Button color="primary" outline>Primary</Button>
<Button color="primary" outline loading>Loading</Button>
<Button color="primary" outline disabled>Disabled</Button>
```

<br /><br />

## Secondary action buttons

Used for second type of actions, like: Subscribe, Submit, Go to Page, Show more, Search & etc.

<br />

### Secondary - High priority

<Button color="secondary" style={{width: 200, margin: 8}}>Secondary</Button>
<Button color="secondary" style={{width: 200, margin: 8}} loading>Loading</Button>
<Button color="secondary" style={{width: 200, margin: 8}} disabled>Disabled</Button>

```
<Button color="secondary">Secondary</Button>
<Button color="secondary" loading>Loading</Button>
<Button color="secondary" disabled>Disabled</Button>
```

<br />

### Secondary - Low priority

<Button color="secondary" outline style={{width: 200, margin: 8}}>Secondary</Button>
<Button color="secondary" outline style={{width: 200, margin: 8}} loading>Loading</Button>
<Button color="secondary" outline style={{width: 200, margin: 8}} disabled>Disabled</Button>

```
<Button color="secondary" outline>Secondary</Button>
<Button color="secondary" outline loading>Loading</Button>
<Button color="secondary" outline disabled>Disabled</Button>
```

<br />

### Warning

<Button color="warning" style={{width: 200, margin: 8}}>Warning</Button>
<Button color="warning" style={{width: 200, margin: 8}} loading>Loading</Button>
<Button color="warning" style={{width: 200, margin: 8}} disabled>Disabled</Button>

```
<Button color="warning">Secondary</Button>
<Button color="warning" loading>Loading</Button>
<Button color="warning" disabled>Disabled</Button>
```

<br />

### Warning - Low priority

<Button color="warning" outline style={{width: 200, margin: 8}}>Warning</Button>
<Button color="warning" outline style={{width: 200, margin: 8}} loading>Loading</Button>
<Button color="warning" outline style={{width: 200, margin: 8}} disabled>Disabled</Button>

```
<Button color="warning" outline>Secondary</Button>
<Button color="warning" outline loading>Loading</Button>
<Button color="warning" outline disabled>Disabled</Button>
```