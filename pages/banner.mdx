import Banner from '../src/Banner/Banner';
import Button from '../src/Button/Button';
import Icon from '../internal/Icons/Error';

# Banner

<br /><br />

## Default With Action

<Banner action={<Button size="small" color="secondary">Update Billing Method</Button>}>
    <b>Payment failed.</b> Please update your billing method.
</Banner>

```
<Banner action={<Button size="small" color="secondary">Update Billing Method</Button>}>
    <b>Payment failed.</b> Please update your billing method.
</Banner>
```

<br /><br />

## Secondary With Action And Icon

<Banner type="secondary" action={<Button size="small" color="primary" outline>Resend Email</Button>} icon={<Icon />}>
    Please confirm your email by clicking the link we sent to <b>email</b>
</Banner>

```
<Banner type="secondary" action={<Button size="small" color="primary" outline>Resend Email</Button>} icon={<Icon />}>
    Please confirm your email by clicking the link we sent to <b>email</b>
</Banner>
```

<br /><br />

## Readonly

<Banner type="readonly">
    <b>Payment failed.</b> Please update your billing method.
</Banner>

```
<Banner type="readonly">
    <b>Payment failed.</b> Please update your billing method.
</Banner>
```
