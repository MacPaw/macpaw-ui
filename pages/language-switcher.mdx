import { useState } from 'react';
import { LanguageSwitcher } from '../src/ui';
import DropdownIcon from '../src/Icons/jsx/DropdownIcon';

# Language Switcher

export const Example = () => {
    const [currentLanguage, setCurrentLanguage] = useState('en');
    return (
        <React.Fragment>
            <LanguageSwitcher
                currentLanguage={currentLanguage}
                availableLanguages={["en", "de", "uk"]}
                onChange={(event) => setCurrentLanguage(event.target.value)}
            />
        </React.Fragment>
    );
};

<Example />

```
const [currentLanguage, setCurrentLanguage] = useState('en');

<LanguageSwitcher
    currentLanguage={currentLanguage}
    availableLanguages={["en", "de", "uk"]}
    onChange={(event) => setCurrentLanguage(event.target.value)}
/>
```
